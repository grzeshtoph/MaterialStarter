'use strict';

describe('myApp.materialView module', function() {

  beforeEach(module('myApp.materialView'));

  describe('materialView controller', function(){
    it('should ....', inject(function($controller) {
      //spec body
      var materialViewCtrl = $controller('MaterialViewCtrl');
      expect(materialViewCtrl).toBeDefined();
    }));

  });
});